{
  "name": "invoice_app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@react-pdf-viewer/core": "^3.12.0",
    "@react-pdf-viewer/default-layout": "^3.12.0",
    "@react-pdf/renderer": "^4.2.1",
    "cra-template": "1.2.0",
    "formik": "^2.4.6",
    "pdfjs-dist": "^3.11.174",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.4.0",
    "react-pdf": "^6.2.2",
    "react-router-dom": "^7.1.5",
    "react-scripts": "5.0.1",
    "react-toastify": "^11.0.3",
    "web-vitals": "^4.2.4"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "description": "1. First of all a login page will open\r 2. Enter any credentials to login. Those credentials will be store in the sessionStorage.\r 3. After login page will be redirect to the invoice ui.\r 4. There add all the values in the fields. Those Fields are validation fields. All Fields are manadatory.\r 5. After filling all values click save button at the bottom. Those values will be saved in the sessionStorage.\r 6. Even after Reload those previous saved values will be re-filled in the fields.\r 7. If any user has not logged in then it will be automatically redirected to login page.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anandkisku/Invoice_Project.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/anandkisku/Invoice_Project/issues"
  }
}
